import React from 'react'

export { FullScreen } from './FullScreen';
export { ScreenSlice } from './ScreenSlice';

/*
ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <FullScreen style={{ backgroundColor: 'blue' }}>
      <ScreenSlice
        direction='v'
        elements={[
          [null, 2, {backgroundColor: 'yellow'}],
          [null, 5, {backgroundColor: 'red'}]
        ]}
        reverse
      />
    </FullScreen>
  </React.StrictMode>,
)
*/
